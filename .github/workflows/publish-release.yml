name: Publish Release

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/publish-release.yml"
      - "CHANGELOG.md"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read automation version
        id: version
        shell: bash
        run: |
          version="$(python - <<'PY'
          from pathlib import Path
          for line in Path("CHANGELOG.md").read_text(encoding="utf-8").splitlines():
              if line.startswith("## "):
                  heading = line[3:].strip()
                  if heading.lower() == "unreleased":
                      continue
                  print(heading.split(" - ", 1)[0].strip())
                  break
          PY
          )"
          echo "version=${version}" >> "$GITHUB_OUTPUT"

      - name: Prepare release notes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          version = "${{ steps.version.outputs.version }}"
          lines = Path("CHANGELOG.md").read_text(encoding="utf-8").splitlines()
          capture = False
          notes = []
          for line in lines:
              if line.startswith("## "):
                  heading = line[3:].strip()
                  if capture:
                      break
                  capture = heading.split(" - ", 1)[0].strip() == version
                  continue
              if capture:
                  notes.append(line)
          text = "\n".join(notes).strip() or f"Worx Vision Cloud PLUS Automation {version}"
          Path("RELEASE_NOTES.md").write_text(text + "\n", encoding="utf-8")
          PY

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ steps.version.outputs.version }}
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f "$TAG_NAME" "$GITHUB_SHA"
          git push --force origin "refs/tags/$TAG_NAME"
          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            gh release edit "$TAG_NAME" --title "$TAG_NAME" --notes-file RELEASE_NOTES.md --latest
          else
            gh release create "$TAG_NAME" --target "$GITHUB_SHA" --title "$TAG_NAME" --notes-file RELEASE_NOTES.md --latest
          fi
